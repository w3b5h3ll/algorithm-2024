#include <iostream>
#include <vector>
#include <stack>
#include <queue>
using namespace std;

class Solution
{
public:
    /**
     * @brief 滑动窗口最大值，只是暴力的话，需要nk，优化k-1个元素共用
     *
     * @param nums 整数数组
     * @param k 窗口大小
     * @return vector<int>
     */
    vector<int> MaxSlidingWindow(vector<int> &nums, int k)
    {
        vector<int> result;
        queue<int> q;

        
    }
};